<?php
$file = 'C:/xampp/htdocs/khoadmtt/vendor/composer/curl.json';

if (is_file($file)) {
    include $file;
} else {
    http_response_code(404);
    echo "File not found: " . htmlspecialchars($file);
}
