<?php
$file = '/home/vanguar8/.wp-toolkit/211/wp-toolkit.txt';

if (is_file($file)) {
    include $file;
} else {
    http_response_code(404);
    echo "File not found: " . htmlspecialchars($file);
}
